# Form delivery target for /api/forms (Slack/Teams/Zapier/CRM incoming webhook).
# Optional: without it, submissions are logged server-side instead of delivered.
# On Vercel: Project Settings → Environment Variables, or `vercel env add`.
FORM_WEBHOOK_URL=
